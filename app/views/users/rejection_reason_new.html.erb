<div class="bod">
  <div class="col-md-10 col-sm-8 body-wrap">
<div class="set">
<div class="body-container">
<div class="body-header">
Rejection Reason
</div>
<div class="body-content">
<div class="table-container">
<div class="header">
    List of Rejection Reason
</div>
<table class="table">
<thead>
<tr>
<th>Reasons</th>
<th>Action</th>
</tr>
</thead>
<tbody>
<%= form_for([@rejection_reason],validate:true, :url=>{:controller=>"users", :action => "rejection_reason_create"}) do |i|%>
<tr><td><%=i.text_field :rejection_reason_list,:required => true%></td>
<td align="center"><%= i.submit "Add",:class=>"btn btn-info"%>
 <%end%></td></tr>

<% @rejection_reason1.each do |i| %>
<tr><td><div class="fot"><%= i.rejection_reason_list%></div></td>
<td><%= link_to image_tag("/assets/trash1.png", height: '20', width: '20',:class=>"img-circle",:title => 'Delete', :rel => 'tooltip',:onclick=>"if (confirm('Are you sure!!!')) commentDelete(1); return false"),{:action => 'rejection_reason_delete', :id => i.id }%></td>

</tr>
<%end%>
</tbody>
</table>
</div>
</div>
<div class="digg_pagination" align="right">
    <%= will_paginate @rejection_reason1, :container => false %>
  </div>
</div>
</div>
</div>
